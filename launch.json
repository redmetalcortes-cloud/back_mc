{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI (debug)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--host", "0.0.0.0", "--port", "8000"],
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "ENV": "dev"  // Esto sobrescribe el .env si coincide la clave
      },
      "jinja": true
    }
  ]
}
